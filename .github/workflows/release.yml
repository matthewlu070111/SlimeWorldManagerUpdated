name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing tag to publish (e.g. v2.3.0). Leave empty to use the pushed tag."
        required: false
        default: ""

permissions:
  contents: write

jobs:
  release:
    name: Build and publish GitHub Release
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve tag name
        id: meta
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" && -n "${{ inputs.tag }}" ]]; then
            TAG="${{ inputs.tag }}"
          elif [[ "${{ github.ref_type }}" == "tag" ]]; then
            TAG="${{ github.ref_name }}"
          else
            echo "No tag available. Push a v* tag or pass workflow_dispatch.tag."
            exit 1
          fi
          # Tag v2.3.0 / v2.3.0-rc1 -> Maven version 2.3.0 / 2.3.0-rc1
          VERSION="${TAG#v}"
          if [[ -z "${VERSION}" ]]; then
            echo "Invalid tag '${TAG}'; expected v* (e.g. v2.3.0)."
            exit 1
          fi
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "Resolved tag=${TAG} mavenVersion=${VERSION}"

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Cache NMS local repo
        uses: actions/cache@v4
        with:
          path: |
            ~/.m2/repository/org/spigotmc/spigot
          key: nms-codemc-minimalpom-v2-${{ runner.os }}-${{ hashFiles('**/pom.xml', '.github/scripts/install-nms.sh') }}
          restore-keys: |
            nms-codemc-minimalpom-v2-${{ runner.os }}-

      - name: Install NMS from CodeMC
        run: |
          chmod +x .github/scripts/install-nms.sh
          .github/scripts/install-nms.sh

      # Align artifact names, plugin.yml version, and POMs with the release tag
      # (otherwise JARs stay named *-2.2.0-SNAPSHOT.jar while the release is e.g. v2.3.0).
      - name: Set Maven version from tag
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${{ steps.meta.outputs.version }}"
          mvn -B versions:set -DnewVersion="${VERSION}" -DgenerateBackupPoms=false -DprocessAllModules=true
          echo "Maven project.version is now ${VERSION}"

      - name: Build with Maven
        run: mvn -B package -DskipTests

      - name: Collect release JARs
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${{ steps.meta.outputs.version }}"
          mkdir -p release-assets
          shopt -s nullglob

          plugin_jar="slimeworldmanager-plugin/target/slimeworldmanager-plugin-${VERSION}.jar"
          classmod_jar="slimeworldmanager-classmodifier/target/slimeworldmanager-classmodifier-${VERSION}.jar"

          if [[ ! -f "${plugin_jar}" ]]; then
            echo "Expected plugin JAR not found: ${plugin_jar}"
            echo "Available:"
            ls -la slimeworldmanager-plugin/target/*.jar 2>/dev/null || true
            exit 1
          fi
          if [[ ! -f "${classmod_jar}" ]]; then
            echo "Expected classmodifier JAR not found: ${classmod_jar}"
            echo "Available:"
            ls -la slimeworldmanager-classmodifier/target/*.jar 2>/dev/null || true
            exit 1
          fi

          cp -v "${plugin_jar}" release-assets/
          cp -v "${classmod_jar}" release-assets/
          ls -la release-assets/

      - name: Generate release notes
        shell: bash
        env:
          TAG: ${{ steps.meta.outputs.tag }}
          VERSION: ${{ steps.meta.outputs.version }}
        run: |
          set -euo pipefail
          PREV="$(git describe --tags --abbrev=0 "${TAG}^" 2>/dev/null || true)"
          if [[ -n "${PREV}" ]]; then
            RANGE_LABEL="Changes since \`${PREV}\`"
          else
            RANGE_LABEL="Recent commits"
          fi

          {
            echo "## Slime World Manager \`${TAG}\`"
            echo
            echo "### Supported versions"
            echo
            echo "| Versions | Compile-time NMS | Runtime |"
            echo "|----------|------------------|---------|"
            echo "| **1.8.8 – 1.17.1** | Spigot | Spigot / Paper |"
            echo "| **1.18+** | — | Use [AdvancedSlimeWorldManager](https://github.com/Paul19988/Advanced-Slime-World-Manager) / ASP |"
            echo
            echo "### Platform policy"
            echo
            echo "- **Compile:** Spigot NMS jars (BuildTools)."
            echo "- **Runtime:** Spigot and Paper of the same Minecraft version."
            echo "- Paper-only method patches are optional (ClassModifier)."
            echo
            echo "### Artifacts"
            echo
            echo "- \`slimeworldmanager-plugin-${VERSION}.jar\` — install into \`plugins/\`"
            echo "- \`slimeworldmanager-classmodifier-${VERSION}.jar\` — optional Java agent"
            echo
            echo "### ${RANGE_LABEL}"
            echo
            if [[ -n "${PREV}" ]]; then
              git log "${PREV}..${TAG}" --pretty=format:'- %s (%h)' --no-merges || true
            else
              git log -n 40 "${TAG}" --pretty=format:'- %s (%h)' --no-merges || true
            fi
            echo
            echo
            echo "### Notable work in this fork"
            echo
            echo "- Fix world unload \`ClassCastException\` (\`CustomChunkLoader\` extends \`ChunkRegionLoader\` on 1.8–1.13)."
            echo "- CI and Release workflows are split (\`ci.yml\` vs \`release.yml\`)."
            echo "- Target support range: **1.8.8 through 1.17.1** (1.18+ deferred to ASWM)."
            echo
            echo "### Install"
            echo
            echo "1. Download the plugin JAR from this release."
            echo "2. Place it in the server \`plugins\` folder."
            echo "3. Restart and verify load/unload for your Minecraft version."
            echo
            echo "---"
            echo "_Auto-generated by GitHub Actions Release workflow from \`${TAG}\` (\`${GITHUB_SHA::7}\`)._"
          } > release-notes.md
          cat release-notes.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.meta.outputs.tag }}
          name: Slime World Manager ${{ steps.meta.outputs.tag }}
          body_path: release-notes.md
          draft: false
          prerelease: ${{ contains(steps.meta.outputs.tag, '-rc') || contains(steps.meta.outputs.tag, '-beta') || contains(steps.meta.outputs.tag, '-alpha') || contains(steps.meta.outputs.tag, 'SNAPSHOT') }}
          files: release-assets/*
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
